import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
  },
  dropdownWrapper: {
    flexBasis: '25%',
  },
  inputWrapper: {
    flexBasis: '75%',
    marginLeft: -1,
  },
});
