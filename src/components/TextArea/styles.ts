import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  inputWrapper: {
    minHeight: 120,
    textAlignVertical: 'top',
    paddingHorizontal: 0,
  },
  inputStyle: {
    paddingTop: 12,
  },
  inputStyleOnIOS: {
    paddingTop: 16,
  },
});
